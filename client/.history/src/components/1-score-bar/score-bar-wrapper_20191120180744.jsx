import React, { Component } from "react";

class Scores extends Component {
  state = {};
  render() {
    return <React.Fragment>{this.props.data.map(el => {})}</React.Fragment>;
  }
}
export default Scores;
