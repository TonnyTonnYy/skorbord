import React, { Component } from 'react';

class Scores extends Component {
    state = {  }
    render() { 
        if(this.props.data.side === "left") {
            return (  );
        } else if(this.props.data.side === "right") {


        }
    }
}
export default Scores;